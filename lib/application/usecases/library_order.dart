/// Order of the top-level items of the grouped library.
enum LibraryOrder {
  /// Most recently updated first, with each group at the position of its most
  /// recently updated member.
  recent,

  /// By displayed title: the entry title, or the label of a group.
  alphabetical,
}
